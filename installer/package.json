{
  "name": "jroute-installer",
  "version": "0.2.0-beta",
  "description": "Cross-platform one-click wizard that installs and runs JRoute (JanitorAI reverse proxy).",
  "license": "MIT",
  "author": "JRoute",
  "private": true,
  "main": "src/main.js",
  "type": "module",
  "scripts": {
    "start": "electron .",
    "lint": "node --check src/main.js && node --check src/preload.js && node --check src/installer/*.js",
    "build": "electron-builder"
  },
  "dependencies": {
    "tar": "^7.4.3"
  },
  "devDependencies": {
    "electron": "^33.0.0",
    "electron-builder": "^25.1.8"
  },
  "build": {
    "extends": "./electron-builder.yml"
  }
}
